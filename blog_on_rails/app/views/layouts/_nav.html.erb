<nav class="navbar navbar-expand-lg navbar navbar-dark bg-dark">
  <%= link_to 'Blog On Rails', root_path, class:"navbar-brand", id:"logo" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%=link_to 'Posts', posts_path, class: 'nav-item nav-link'%>



      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
              aria-haspopup="true" aria-expanded="false">
              Actions
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%=link_to 'New Post', new_post_path, class: 'dropdown-item'%>
              <!-- <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#">Something else here</a> -->
            </div>
          </li>
          <!-- <li class="nav-item">
          <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
        </li> -->
        </ul>

        <form class="form-inline my-2 my-lg-0">
          <% if user_signed_in? %>
          <small
            class="mr-2"><%= link_to "EDIT profile",edit_user_path(current_user),class: 'btn btn-outline-success my-2 my-sm-0 mr-2' %></small>
          </li>
          <li class="nav-item ml-2">
            <small>
              <%= 
              link_to 'Sign Out', 
              session_path, 
              method: :delete,
              class: 'btn btn-outline-danger my-2 my-sm-0 mr-2', 
              data: { confirm: "Log out?"}
            %>
            </small>
            <% else %>
            <small>
              <%= link_to 'Login', new_session_path, { class: "navbar-link mx-1" } %>
              <%= link_to 'Sign Up', new_user_path, { class: "navbar-link mx-1" } %>
            </small>
            <% end %>
        </form>

      </div>
</nav>